ibapi==9.81.1.post1
numpy==1.22.4
pandas==1.3.5
pyarrow==12.0.1
PyYAML==6.0.1

# Optional: PostgreSQL sink for the FinStream platform database.
# The parquet datasets are the primary output; the pipeline runs without it.
psycopg2-binary==2.9.9
