# Pin versions compatible with our production stack (Python 3.8, Airflow
# 2.8.1, PySpark 3.5.1, pandas 1.3.5, numpy 1.22.4). Verify any new
# library's Python 3.8 compatibility before adding it here.
#
# Union of both pipelines' dependencies (see pipeline/requirements.txt
# for fundamentals-only, pipeline/indicators/requirements.txt for
# indicators-only -- each Docker image installs only its own scoped
# file, this top-level one is the full picture for a non-containerised
# clean-clone setup covering both).

pyyaml==6.0.1
requests==2.31.0
psycopg2-binary==2.9.9   # read-only access to the shared price_data/companies (postgres) tables
pymongo==4.6.3            # MongoDB output
pyspark==3.5.1            # indicators pipeline only -- OHLCV rolling-window computation
pandas==1.3.5             # indicators pipeline only -- recursive/smoothed indicators
numpy==1.22.4             # indicators pipeline only
