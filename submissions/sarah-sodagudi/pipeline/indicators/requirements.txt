# Pin versions compatible with our production stack (Python 3.8,
# Airflow 2.8.1, PySpark 3.5.1, pandas 1.3.5, numpy 1.22.4).
# Verify any new library's Python 3.8 compatibility before adding it here.
#
# Unlike the fundamentals pipeline, this one needs PySpark + pandas for
# OHLCV rolling-window / recursive-smoothing computation -- see
# indicator_calculator.py / pandas_indicators.py. PySpark also requires a
# real JRE on the host/container (see Dockerfile) -- the pip package
# alone is not enough.

pyyaml==6.0.1
requests==2.31.0
psycopg2-binary==2.9.9   # read-only access to the shared price_data/companies tables
pymongo==4.6.3            # MongoDB output
pyspark==3.5.1
pandas==1.3.5
numpy==1.22.4
